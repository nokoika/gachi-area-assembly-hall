{
  "tasks": {
    "dev": "cp env/.env.local env/.env && deno run --watch --allow-env --allow-net=discord.com,gateway.discord.gg --allow-read --allow-write=./.data src/main.ts",
    "cache": "deno cache src/deps/*",
    "test": "deno test",
    "typecheck": "deno cache main.ts"
  }
}
