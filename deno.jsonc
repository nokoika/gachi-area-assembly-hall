{
  "tasks": {
    "dev": "cp env/.env.local env/.env && deno run --watch --allow-env --allow-net=discord.com,gateway.discord.gg --allow-read src/main.ts",
    "cache": "deno cache src/deps/*",
    "test": "RUST_BACKTRACE=1 deno test --allow-env --allow-read",
    "typecheck": "deno cache main.ts"
  }
}
